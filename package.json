{
  "name": "pondlife",
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "@weborigami/origami": "0.0.70"
  },
  "license": "MIT",
  "engines": {
    "node": "^22.0.0"
  },
  "scripts": {
    "build": "ori tree:copy src/site.ori, tree:clean files:build",
    "docs": "ori '@svg docs/start.yaml, { createLinks: false }' > docs/start.svg; ori '@svg docs/end.yaml, { createLinks: false }' > docs/end.svg",
    "start": "ori dev:serve dev:watch src, =dev:debug site.ori"
  }
}
