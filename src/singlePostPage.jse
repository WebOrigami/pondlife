// A page showing a single post
(post, key, tree) => <page.jse>({
  title: post.title
  __body__: Tree.indent`
    ${ <postFragment.jse>(post, key) }
    <p>
      ${ post.previousKey
          ? Tree.indent`
            <a class="previous" href="${ post.previousKey }">Previous: ${ tree[post.previousKey].title }</a>
            &nbsp;
          `
          : ""
      }
      ${ post.nextKey
          ? Tree.indent`
            <a class="next" href="${ post.nextKey }">Next: ${ tree[post.nextKey].title }</a>
          `
          : Tree.indent`
            <a class="next" href="/">
              Back to home
            </a>
          `
      }
    </p>
` })
